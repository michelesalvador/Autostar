import { FormattaEuroPipe } from './formatta-euro.pipe';

describe('FormattaEuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FormattaEuroPipe();
    expect(pipe).toBeTruthy();
  });
});
